[ ] More than one schedule to create from.
    The DHL report has "WEEKDAY NIGHT", "FRIDAY NIGHT", and "SUNDAY". And it
    auto-selects them.
    The Amazon report has only one "Contracted". It does not auto-select.
[ ] Categories per flight per company (Type in mdb).
[ ] Flights have legs and they appear to be used.
[ ] Inject variables into global javascript variables.

DHL Report

[ ] Flights have in/out bound attribute.
    inbound is the front half and outbound is the back half.
    Scheduled flights also have in/out bound.
[ ] Flight types are "Scheduled", "Extra", "Extra CMI", "Extra Non-CMI".

DHL Report Legacy

* The `details` table simply exists like an entity table. Several other tables add attributes.
*

# INCIDENTS

* #16585 hide delays from report for config LATE_GT.
